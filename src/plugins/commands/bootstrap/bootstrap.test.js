'use strict';

/* global expect */

describe('Command bootstrap', () => {

    it('bootstrap run', async () => {

        // const { service } = require('../../../../');

        // await service.run('bootstrap');

    });

});
